Algoritmo "Não exibir número maior que limite fixo"

Var
    numero: real


Inicio
escreval("Se Número maior que tal não aparece na tela")
leia (numero)


    se numero > 10 então
        escreval("O número não será exibido, pois é maior que o limite estabelecido.")
    senao
        escreval("O número informado é: ", numero)
    fimse

Fimalgoritmo